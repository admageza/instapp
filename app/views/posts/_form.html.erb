<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<ul class="row col-sm-12">
  <div class="field" style="margin-top:2%">
     <li class="col-sm-2"><%= form.label :image %></li>
     <li class="col-sm-4"><%= form.file_field :image, id: :post_image %></li>
     <li class="col-sm-6"><%= form.hidden_field :image_cache %></li>
  
  </div>

  <div class="actions" style="margin-top:3%">
    <li class="col-sm-10"style="margin-left:50%"><%= form.submit %></li>
  </div>
</ul>
<% end %>
